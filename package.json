{
	"name": "clone-anywhere-for-github",
	"version": "1.2.0",
	"description": "A Chrome extension that clones any GitHub repo into VS Code, Cursor, Windsurf, VSCodium, or Insiders — or downloads it as a .zip, straight from the repo page.",
	"main": "dist/content.js",
	"scripts": {
		"bundle": "esbuild src/content.ts src/popup.ts --bundle --format=iife --target=es2020 --outdir=dist",
		"build": "npm run bundle && node scripts/build-manifest.mjs && cp popup.html popup.css dist/ && cp -r icons dist/",
		"dev": "npm run bundle -- --watch",
		"clean": "rm -rf dist",
		"package": "npm run build && cd dist && zip -r \"../clone-anywhere-for-github-v$npm_package_version.zip\" .",
		"lint": "eslint .",
		"typecheck": "tsc --noEmit && tsc -p e2e",
		"test": "vitest run",
		"format": "prettier --write \"src/**/*.ts\" \"e2e/**/*.ts\" playwright.config.ts",
		"format:check": "prettier --check \"src/**/*.ts\" \"e2e/**/*.ts\" playwright.config.ts",
		"e2e": "playwright test"
	},
	"keywords": [
		"chrome-extension",
		"vscode",
		"github",
		"typescript",
		"manifest-v3"
	],
	"author": "AdamAkhlaq",
	"license": "MIT",
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@playwright/test": "^1.63.0",
		"@types/chrome": "^0.1.1",
		"@types/node": "^26.6.2",
		"esbuild": "^0.28.1",
		"eslint": "^10.5.0",
		"eslint-config-prettier": "^10.1.8",
		"globals": "^17.6.0",
		"jsdom": "^29.1.1",
		"prettier": "^3.8.4",
		"typescript": "^5.8.3",
		"typescript-eslint": "^8.61.0",
		"vitest": "^4.1.9"
	}
}
